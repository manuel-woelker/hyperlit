[package]
name = "hyperlit_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
hyperlit_base = { path = "../hyperlit_base" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
tracing = "0.1"
